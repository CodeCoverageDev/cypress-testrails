describe('C1 User can login', () => {
    it('user navigates to example.com', () => {
        cy.wait(1000);
    });

    it('enter the email', () => {
        cy.wait(1000);
    });

    it('enter the password', () => {
        cy.wait(1000);
    });

    it('click the login', () => {
        cy.wait(1000);
    });

    it('should be able to see the User Dashboard', () => {
        cy.wait(1000);
    });
});