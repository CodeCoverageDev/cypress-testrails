describe('C2 User can logout', () => {
    it('user login', () => {
        cy.wait(1000);
    });

    it('click the profile icon', () => {
        cy.wait(1000);
    });

    it('click logout', () => {
        cy.wait(1000);
    });

    it('should be redirected to the login screen', () => {
        cy.wait(1000);
    });
});